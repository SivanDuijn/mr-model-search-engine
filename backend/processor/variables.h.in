#pragma once

#include <string.h>

#define V_ROOT_PATH "@ASSETS_PATH@"

namespace vars 
{
    inline std::string GetAssetPath()
    {
        std::string root(V_ROOT_PATH);
        return root + "/../../frontend/model-viewer/public/";
    }
}